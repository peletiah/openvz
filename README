check_beancounter.py -- A nagios plugin to check changes in /proc/user_beancounters:
	
Usage: check_beancounter.py [options]

Options:
  -h, --help  show this help message and exit
  -i          Check if failcnt-values have increased since the last run and
              send mail if true, requires -e
  -e EMAIL    The email address of the warning recipient, required for -i
  -c          Check if current value of a resource is higher than
              barrier/limit
